{
  "name": "grodt-website",
  "version": "1.0.0",
  "private": true,
  "description": "Get Ripped Or Die Trying — Detroit fitness apparel",
  "scripts": {
    "build": "sh -c 'test -f public/index.html && echo \"Build OK: public/index.html found\"'",
    "start": "python3 -m http.server 8080 --directory public"
  },
  "dependencies": {
    "stripe": "^17.7.0"
  }
}
