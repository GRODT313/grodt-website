{
  "name": "grodt-website",
  "version": "1.0.0",
  "private": true,
  "description": "Get Ripped Or Die Trying — Detroit fitness apparel",
  "scripts": {
    "build": "node -e \"console.log('Static site — no build step required.')\"",
    "dev": "vercel dev",
    "start": "python3 -m http.server 8080"
  },
  "dependencies": {
    "stripe": "^17.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
