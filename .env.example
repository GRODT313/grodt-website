# Stripe secret key from https://dashboard.stripe.com/apikeys
# Use sk_test_... while testing, sk_live_... for real payments.
# Add this same value in Vercel → Project → Settings → Environment Variables.
STRIPE_SECRET_KEY=sk_test_your_key_here

# Optional: override First 50 set limit (default 50)
# FIRST50_LIMIT=50
